name: audio_tags_interface
description: >
  Core interface and data models for audio_tags backends. Backend authors
  depend on this; end users depend on audio_tags instead.
version: 0.1.0
publish_to: https://myclub.birju.dev
repository: https://github.com/birjuvachhani/audio_tags
issue_tracker: https://github.com/birjuvachhani/audio_tags/issues

topics:
  - audio
  - metadata
  - id3
  - tags
  - flac

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  meta: ^1.17.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.0
